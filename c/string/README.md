# 字符串章节示例

本目录包含C语言字符串处理的完整示例代码，从基础到高级，涵盖字符串的各个方面。

## 📁 文件列表

### 新增示例
- **string_basics.c** - 字符串基础
  - 字符串基本概念
  - 字符串声明和初始化
  - 字符串访问和修改
  - 字符串长度和大小
  - 字符串字面量
  - 字符串与字符数组的区别
  - 常见错误和陷阱
  - 字符串遍历方法

- **string_operations.c** - 字符串操作函数
  - 字符串复制（strcpy, strncpy, memcpy）
  - 字符串连接（strcat, strncat）
  - 字符串比较（strcmp, strncmp）
  - 字符串搜索（strchr, strrchr, strstr）
  - 字符串分割（strtok）
  - 字符串长度（strlen）
  - 内存操作（memset, memcpy, memmove, memcmp, memchr）
  - 实用字符串操作

- **string_manipulation.c** - 字符串处理技巧
  - 安全字符串操作
  - 字符串格式化和构建
  - 大小写转换
  - 字符串修剪（去除空白）
  - 字符串替换
  - 字符串分割
  - 字符串反转
  - 回文检查
  - 统计子字符串
  - 单词计数
  - 前缀和后缀检查

### 原有示例
- **test.c** - 简单的字符串测试

## 🎯 学习路径

建议按以下顺序学习：

1. **string_basics.c** - 掌握字符串的基本概念
2. **string_operations.c** - 学习标准库函数
3. **string_manipulation.c** - 掌握实用技巧

## 🚀 编译和运行

### 编译单个文件

```bash
# 编译基础示例
cc -std=c17 -Wall -Wextra string_basics.c -o string_basics
./string_basics

# 编译操作示例
cc -std=c17 -Wall -Wextra string_operations.c -o string_operations
./string_operations

# 编译处理技巧示例
cc -std=c17 -Wall -Wextra string_manipulation.c -o string_manipulation
./string_manipulation
```

### 使用父目录的Makefile

```bash
# 在c目录下编译所有示例
cd ..
make all

# 运行编译后的程序
./bin/string_string_basics
./bin/string_string_operations
./bin/string_string_manipulation
```

## 📚 知识点总结

### 1. 字符串基础

C语言中的字符串：
- 字符串是以 `'\0'` 结尾的字符数组
- 字符串字面量是只读的
- 字符串长度不包括 `'\0'`
- 数组大小包括 `'\0'`

```c
char str[] = "Hello";  // 长度5，大小6（包括'\0'）
```

### 2. 字符串声明和初始化

```c
char str1[] = "Hello";                    // 自动确定大小
char str2[10] = "Hello";                  // 指定大小
char str3[] = {'H', 'e', 'l', 'l', 'o', '\0'};  // 字符数组
const char *str4 = "String Literal";      // 字符串指针
```

### 3. 常用字符串函数

| 函数 | 功能 | 示例 |
|------|------|------|
| `strlen` | 获取字符串长度 | `strlen("Hello")` → 5 |
| `strcpy` | 复制字符串 | `strcpy(dest, src)` |
| `strncpy` | 复制指定长度 | `strncpy(dest, src, n)` |
| `strcat` | 连接字符串 | `strcat(dest, src)` |
| `strncat` | 连接指定长度 | `strncat(dest, src, n)` |
| `strcmp` | 比较字符串 | `strcmp(s1, s2)` |
| `strncmp` | 比较前n个字符 | `strncmp(s1, s2, n)` |
| `strchr` | 查找字符 | `strchr(str, 'c')` |
| `strstr` | 查找子字符串 | `strstr(str, "sub")` |
| `strtok` | 分割字符串 | `strtok(str, ",")` |

### 4. 内存操作函数

| 函数 | 功能 | 用途 |
|------|------|------|
| `memset` | 设置内存 | 初始化内存 |
| `memcpy` | 复制内存 | 快速复制（不可重叠） |
| `memmove` | 移动内存 | 复制（可重叠） |
| `memcmp` | 比较内存 | 比较字节序列 |
| `memchr` | 查找字节 | 在内存中查找 |

### 5. 安全字符串操作

```c
// 不安全
strcpy(dest, src);  // 可能溢出

// 安全
strncpy(dest, src, sizeof(dest) - 1);
dest[sizeof(dest) - 1] = '\0';

// 更安全
snprintf(dest, sizeof(dest), "%s", src);
```

## ⚠️ 常见陷阱

1. **缓冲区溢出**
   ```c
   char buf[5];
   strcpy(buf, "Hello, World!");  // 溢出！
   ```

2. **忘记 '\0' 结束符**
   ```c
   char str[5] = {'H', 'e', 'l', 'l', 'o'};  // 没有'\0'！
   printf("%s", str);  // 未定义行为
   ```

3. **修改字符串字面量**
   ```c
   char *str = "Hello";
   str[0] = 'h';  // 运行时错误！
   ```

4. **strncpy 不自动添加 '\0'**
   ```c
   char buf[5];
   strncpy(buf, "Hello", 5);  // buf 可能没有'\0'！
   buf[4] = '\0';  // 手动添加
   ```

5. **strtok 修改原字符串**
   ```c
   char *str = "a,b,c";
   strtok(str, ",");  // 错误！字符串字面量是只读的
   
   char copy[] = "a,b,c";
   strtok(copy, ",");  // 正确
   ```

## 💡 最佳实践

1. **总是使用安全函数**
   - 优先使用 `strncpy`, `strncat`, `snprintf`
   - 检查缓冲区大小
   - 确保字符串以 `'\0'` 结尾

2. **字符串复制**
   ```c
   // 好的做法
   strncpy(dest, src, sizeof(dest) - 1);
   dest[sizeof(dest) - 1] = '\0';
   ```

3. **字符串拼接**
   ```c
   // 好的做法
   snprintf(dest, sizeof(dest), "%s%s", str1, str2);
   ```

4. **使用 const**
   ```c
   // 只读字符串使用 const
   const char *readonly = "String Literal";
   ```

5. **检查返回值**
   ```c
   char *result = strchr(str, 'x');
   if (result != NULL) {
       // 找到了
   }
   ```

## 🔗 高级技巧

### 1. 大小写转换

```c
for (int i = 0; str[i]; i++) {
    str[i] = toupper((unsigned char)str[i]);
}
```

### 2. 去除空白

```c
// 去除前导空格
while (isspace(*str)) str++;

// 去除尾随空格
char *end = str + strlen(str) - 1;
while (end > str && isspace(*end)) end--;
*(end + 1) = '\0';
```

### 3. 字符串反转

```c
size_t len = strlen(str);
for (size_t i = 0; i < len / 2; i++) {
    char temp = str[i];
    str[i] = str[len - 1 - i];
    str[len - 1 - i] = temp;
}
```

### 4. 字符串分割

```c
char *token = strtok(str, ",");
while (token != NULL) {
    printf("%s\n", token);
    token = strtok(NULL, ",");
}
```

### 5. 检查回文

```c
bool is_palindrome(const char *str) {
    size_t len = strlen(str);
    for (size_t i = 0; i < len / 2; i++) {
        if (str[i] != str[len - 1 - i]) {
            return false;
        }
    }
    return true;
}
```

## 📖 参考资源

- C17/C18 标准文档 - string.h
- 《C Primer Plus》第11章 - 字符串和字符串函数
- 《C程序设计语言》（K&R）第5章 - 指针与数组
- POSIX字符串函数扩展

## 🔍 相关主题

- **数组** - 字符串是字符数组
- **指针** - 字符串操作常用指针
- **内存管理** - 动态字符串需要内存管理
- **字符分类** - ctype.h 中的字符处理函数

---

**标准**: C17/C18  
**编译器**: GCC, Clang, MSVC (支持C99及以上)  
**特别说明**: 注意字符串安全，避免缓冲区溢出
