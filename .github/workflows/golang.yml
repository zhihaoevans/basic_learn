name: golang

on:
  push:
    paths:
      - 'golang/**'
      - 'languages/go/**'
      - '.github/workflows/golang.yml'
  pull_request:
    paths:
      - 'golang/**'
      - 'languages/go/**'
      - '.github/workflows/golang.yml'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Go version
        run: go version

      - name: Vet and run every example
        run: |
          cd golang
          for f in *.go; do
            echo "== $f =="
            go vet "$f"
            go run "$f"
          done

      # languages/go 为早期章节式练习（go.mod 已补齐），以单元测试验证
      - name: Test languages/go
        run: |
          cd languages/go
          go test ./...
