name: lua

on:
  push:
    paths:
      - 'lua/**'
      - '.github/workflows/lua.yml'
  pull_request:
    paths:
      - 'lua/**'
      - '.github/workflows/lua.yml'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Lua
        run: |
          sudo apt-get update
          sudo apt-get install -y lua5.4

      - name: Run every example
        run: |
          find lua -name '*.lua' | sort | while read -r f; do
            echo "== $f =="
            lua5.4 "$f"
          done
