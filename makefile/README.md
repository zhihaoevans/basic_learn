# Makefile 模板

本目录提供常用 Makefile 模板示例，便于快速启动 C/C++ 或内核模块项目。

## 目录结构

```
makefile/
└── templates/
    ├── c.mk    # C 项目模板
    ├── cpp.mk  # C++ 项目模板
    └── lkm.mk  # Linux 内核模块模板
```

## 使用方式

```bash
# 复制模板到你的项目根目录
cp templates/c.mk ./Makefile
```
